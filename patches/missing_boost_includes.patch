--- a/CMakeLists.txt
+++ b/CMakeLists.txt
@@ -144,6 +144,8 @@ if (HEAPTRACK_BUILD_TRACK)
     endif()
 endif()

+include_directories(${Boost_INCLUDE_DIRS})
+
 add_subdirectory(3rdparty)
 add_subdirectory(src)
 add_subdirectory(tests)
